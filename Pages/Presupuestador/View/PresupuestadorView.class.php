<?php
/**
 * Generada por workbench.php ()
 * clase PresupuestadorView (PresupuestadorView.class.php)
 *
 */
 namespace SkyData\Pages\Presupuestador\View;
 
 use \SkyData\Core\Page\View\SkyDataPageView;
 
 class PresupuestadorView extends SkyDataPageView
 {
 	public function Render ()
 	{
 		// Importante: Siempre se ha de llamar al método del parent, a menos que se desee manipular el template directamente
 		// Asigne cualquier variable que se desee antes de llamar el método parent.
 		// Ej:
 		// \$this->Assign ('var', \$value);
 		// 
	
 		return parent::Render();
 	}
 }
