{
   "query": {
     "match": {
       "id_concesionario": "{{id_concesionario}}"
     }
     
   },
   "aggs": {
     "categorias": {
       "terms": {
         "script": "_source.crm_module == 'Contacts' ? (_source.categoria != null ? _source.categoria : 'No informado') : null "
       }
     }
   },
   "size": 0
}