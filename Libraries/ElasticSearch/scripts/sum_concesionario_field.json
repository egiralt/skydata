{
    "query": {
        "match": {
           "id_concesionario": "{{id_concesionario}}"
        }
    }, 
   "aggs": {
      "sum_field": {
         "sum": {
            "field": "{{field}}"
         }
      }
   },
   "size": 0
}