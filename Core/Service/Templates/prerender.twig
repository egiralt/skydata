{% macro generate(method) %}

	{% if method.preRenderView|length > 0 %}
		$http.get ('{{ method.preRenderView }}')
			.then (function (response) 
			{
				$($element).html(response.data);
			});
	{% endif %}
	
{% endmacro %}