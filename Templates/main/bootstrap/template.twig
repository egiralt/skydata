<!DOCTYPE html>
<html  xmlns:ng="http://angularjs.org">
    <head>
		{% include 'html_head.twig' %}
    </head>
    	
	<body class="no-right-bar" ng-app='SkyDataApp' ng-controller='SkyDataAppController'>
		<div class="wrap-general height-wrapper">
			{# TODO: Incluir la validación para saber si está autenticado #}
			{% include 'header.twig' %}
			{% include 'main.twig' %}
		</div>
		
		<!-- Script de la página. Al final para que la página se descargue rápido -->
		{{ page_scripts | raw }}
    </body>
</html>
	